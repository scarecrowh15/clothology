<p id="notice"><%= notice %></p>
<%= render 'layouts/navbar' %>

<div class="article-title-main">
    <h1><strong> <%= @brand.article_title %> </strong> </h1>
</div>

<div class="article-overview-main">
    <h5> <%= @brand.article_overview %> </h5>
</div>


<div class="article-image">
  <div id="carouselExampleIndicators" class="carousel slide" data-ride="carousel">

  <div class="carousel-inner">
  <% if @brand.article_image1.present? %>
  <div class="carousel-item active">
    <img class="d-block w-100" src=<%= @brand.article_image1_url %> alt="First slide">
  </div>
  <% end %>
  <% if @brand.article_image2.present? %>
    <div class="carousel-item">
      <img class="d-block w-100" src=<%= @brand.article_image2_url %> alt="First slide">
    </div>
  <% end %>
  <% if @brand.article_image3.present? %>
    <div class="carousel-item">
      <img class="d-block w-100" src=<%= @brand.article_image3_url %> alt="First slide">
    </div>
  <% end %>
  <a class="carousel-control-prev" href="#carouselExampleIndicators" role="button" data-slide="prev">
    <span class="carousel-control-prev-icon" aria-hidden="true"></span>
    <span class="sr-only">Previous</span>
  </a>
  <a class="carousel-control-next" href="#carouselExampleIndicators" role="button" data-slide="next">
    <span class="carousel-control-next-icon" aria-hidden="true"></span>
    <span class="sr-only">Next</span>
  </a>
  </div>

</div>

<div class="article-content">
  <p> <%= @brand.article_content.html_safe %> </p>
</div>


<%= render 'layouts/footer' %>